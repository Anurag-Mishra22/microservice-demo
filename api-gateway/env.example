# PostgreSQL Database Configuration
DB_HOST=localhost
DB_PORT=5432
DB_NAME=dbname
DB_USER=root
DB_PASSWORD=password
DB_SSL_MODE=disable

# Alternative: Full DATABASE_URL (uncomment to use)
# DATABASE_URL=postgres://username:password@localhost:5432/dbname?sslmode=disable 